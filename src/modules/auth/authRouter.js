const express = require("express");
const Router = express();
const authController = require("../auth/authController");

// Router.get("/activate-email/:id", authController.activateEmail);

module.exports = Router;
